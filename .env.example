# Gemini API Key (required)
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_api_key_here

# Checkout Directory (required)
# Base directory where project repositories will be cloned
CHECKOUT_DIR=/path/to/checkout

# Server Configuration (optional)
PORT=3000

# History Directory (optional)
# Directory to store request/response history with timestamps
# If not set, history logging will be disabled
# HISTORY_DIR=/path/to/history

# Session Secret (optional)
# Will be auto-generated if not provided
# SESSION_SECRET=your-secret-here

# Security Configuration (optional)
# Set NODE_ENV to 'production' to enable secure cookies for HTTPS
# NODE_ENV=production

# Force secure cookies even if NODE_ENV is not 'production'
# SECURE_COOKIES=true

# Set cookie domain for multi-domain setups (optional)
# COOKIE_DOMAIN=promptly.snowmonkey.co.uk
